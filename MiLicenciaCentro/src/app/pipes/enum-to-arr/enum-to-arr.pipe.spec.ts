import { EnumToArrPipe } from './enum-to-arr.pipe';

describe('EnumToArrPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumToArrPipe();
    expect(pipe).toBeTruthy();
  });
});
