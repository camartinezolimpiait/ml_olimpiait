/**
 * Selección de trámite de licencia.
 * Migrado desde Angular: src/app/interfaces/compraPin/SeleccionTramite.ts
 */
export interface SeleccionTramite {
  id: number;
  nombre: string;
  instructor: boolean;
  selected: boolean;
}
