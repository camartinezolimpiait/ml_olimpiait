/**
 * Convenio disponible en un centro.
 * Migrado desde Angular: src/app/interfaces/cotizacion/Convenio.ts
 */
export interface Convenio {
  id: number;
  nombre: string;
  descripcion?: string;
  descuento?: number;
}
