# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '14.x'
  displayName: 'Install Node.js'

- script: |
    cd MiLicenciaCentro
    npm install
    npm run build
  displayName: 'npm install and build'

- task: ArchiveFiles@2
  displayName: 'Archive files'
  inputs:
    rootFolderOrFile: '$(System.DefaultWorkingDirectory)/MiLicenciaCentro/dist/'
    includeRootFolder: false
    archiveType: zip
    archiveFile: $(Build.ArtifactStagingDirectory)/MiLicenciaCentro.zip
    replaceExistingArchive: true

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)/MiLicenciaCentro.zip'
    ArtifactName: 'drop'
    publishLocation: 'Container'
    
# - task: CopyPublishBuildArtifacts@1  
#   displayName: 'Copy Publish Artifact: MiLicenciCentro'  
#   inputs:  
#     CopyRoot: dist  
#     Contents: '**'  
#     ArtifactName: MiLicenciCentro  
#     ArtifactType: Container 
